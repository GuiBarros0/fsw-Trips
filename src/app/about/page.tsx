import React, { useState } from "react";
import Trips from "./components/Trips";

export const metadata = {
    title: "About",
}

const Page = () => {
    return (
        
        <div>
            <Trips/>
        </div>
    )
}

export default Page;